<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.accordmall.mapper.AddressMapper">

    <select id="selectAddressByUserid" parameterType="java.lang.Integer" resultType="cn.accordmall.pojo.model.Address">
        select * from address where user_id = #{user_id}
    </select>

</mapper>
